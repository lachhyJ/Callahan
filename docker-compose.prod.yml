services:
  backend:
    build: ./backend
    restart: unless-stopped
    env_file:
      - backend.prod.env
    volumes:
      - /mnt/tank/callahan-data:/app/App_Data

  frontend:
    build:
      context: ./frontend
      args:
        VITE_API_BASE: ""
    restart: unless-stopped
    ports:
      - "30070:80"
    depends_on:
      - backend

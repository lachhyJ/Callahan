bin/

# Bakes in a real 30-day JWT via scripts/garmin-watch-token/bake-and-build.sh
# — never commit the real one. The .example files are the tracked
# placeholder/templates; bake-and-build.sh copies them on first setup.
resources/settings/properties.xml
source/GeneratedAuthToken.mc
